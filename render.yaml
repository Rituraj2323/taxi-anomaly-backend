services:
  - type: web
    name: taxi-anomaly-api
    env: python
    buildCommand: ./build.sh
    startCommand: gunicorn core.wsgi:application
    envVars:
      - key: MONGODB_URI
        sync: false # You will paste this in the Render UI
      - key: PYTHON_VERSION
        value: 3.9.18
